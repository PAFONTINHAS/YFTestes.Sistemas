//CRIE O BANCO DE DADOS NO PHPMYADMIN(NÃO PRECISA CRIAR A TABELA)
//ENTRE NO BANCO DE DADOS E APERTE NA COLUNA "SQL", O QUAL APARARECERÁ UM EDITOR PARA A LINGUAGEM SQL

//CASO ESTEJA NO MYSQL WORKBENCH, CRIAR O BANCO DE DADOS VIA CÓDIGO

CREATE DATABASE yftestessistemas;

USE yftestessistemas

// COPIE O CÓDIGO ABAIXO E COLAR NO EDITOR

CREATE TABLE caddesp(

    id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    nome VARCHAR(100) NOT NULL,
    categoria VARCHAR(100) NOT NULL,
    valor DECIMAL(10,2) NOT NULL,
    vencimento DATE NOT NULL,
    formapag VARCHAR(100) NOT NULL,
    imovelassoc VARCHAR(100) NOT NULL,
    parcela INT NOT NULL,
    pago TINYINT NOT NULL DEFAULT(0),
    infocomp TEXT NULL,
);

CREATE TABLE cadrec(

    id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    tiporec VARCHAR(100) NOT NULL,
    tiporecebe VARCHAR(100) NOT NULL,
    valorrec DECIMAL(10,2) NOT NULL,
    datarecebe DATE NOT NULL,
    recebe VARCHAR(100) NOT NULL,
    infocomp TEXT NULL
);

//APERTE EM CONTINUAR