-FAZER COM QUE O MODAL SEJA FECHADO QUANDO CLICO FORA DELE OU APERTANDO O excluirDespesa

-TENTAR SER FELIZ

Mano, eu vou ter que pedir para você fazer algumas alterações na página de cadastro de despesas. A primeira delas é mudar os names dos inputs para mim e também eu quero que vc mude todos os inputs que possuem o comando <datalist> e substitua eles para o comando <select>. Outra coisa também, eu quero que vc mude:

O input Nome da Despesa, eu quero que vc troque o <textarea> e deixe como input type = text. Quero que vc troque o name do input para "nome";

No input categoria, remova o input text e só deixe o comando <select>, igual está no arquivo que eu te mandei.Quero que vc troque o name do <select> para "categoria";

O input Valor da Despesa e deixe ele como text, porque eu vou aplicar uma máscara Javascript nele e vai ficar bem bacana. Quero que vc troque o name do input para "valor";

No input Forma de Pagamento, remova o input e só deixe o comando <select>. Quero que vc troque o name do <select> para "formaPagamento";

No input Parcelas, remova o input e só deixe o comando <select>. Quero que vc troque o name do <select> para "parcelas";

No input Imovel Associado, remova o input e só deixe o comando <select>. Quero que vc troque o name do <select> para "imovelAssociado";

O input informações complementares pode deixar como textarea mesmo. Quero que vc troque o name do input para "infoComplementares".

é importante que os names sejam exatamente do jeito que eu coloquei aqui. Eu até faria isso, mas como vc está com essa parte do front e também fez todo o css, vai saber melhor o que pode alterar para que não haja nenhum bug.

Se tiver alguma dúvida do que tiver que alterar, dá uma olhadinha no arquivo que eu te mandei que lá já tem um modelo de como fazer os inputs.
